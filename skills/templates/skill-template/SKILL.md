---
name: skill-template
description: 这是一个技能模板占位描述。复制本文件夹后请重写：同时说清「做什么」与「何时用」，列出用户可能说的具体触发短语与相关文件类型，并略带主动性（例：只要用户提到 X / 做 Y，即使没明确说出，也应使用本技能）。≤1024 字符，不含尖括号。
---

# Skill 名称

> 用一两句话概述这个技能让模型能做什么、大致怎么做。
> 编写规范见 `docs/standards/skill-design.md`。完成前对照其「质量检查清单」逐条核对。

## 何时使用（触发说明）

> 提示：所有「何时用」的判断信息应主要放在 frontmatter 的 `description` 里（那是触发机制）。此处仅补充说明。

- 场景 1：……
- 场景 2：……
- 不适用（负向）：……时请改用 ……

## 工作流程（用祈使句 + 解释为什么）

1. 第一步：做……（因为……，所以这样做能……）
2. 第二步：做……
3. 第三步：做……

## 输出格式

始终使用以下结构：

```
# [标题]
## 小节 A
## 小节 B
```

## 示例

**Example 1:**
Input: <用户输入>
Output: <期望输出>

## 资源引用（渐进式披露）

- 需要 <详细规则> 时，阅读 `references/example-reference.md`。
- 执行 <重复性任务> 时，运行 `scripts/`（把可复用逻辑写成脚本，避免每次重造轮子）。
- 输出用到的模板/图标放 `assets/`。

## 注意

- 遵守"无意外原则"：行为应与本文件描述一致，不做用户意料之外的事。
- 保持精简：删掉不拉动价值的内容；接近 500 行时增加分层与指针。
