---
description: 规范驱动开发(SDD)流程约束，涉及新功能/需求变更时应用
alwaysApply: false
---

# 规范驱动开发流程（SDD）

当用户请求新增功能、修改功能行为或规划工作时，遵循此流程。

## 生命周期

```
Constitution → Specify → Clarify → Plan → Tasks → Implement → Validate
   宪法          规格       澄清      计划     拆分      实现       验证
```

1. **Specify（规格）**：在 `specs/NNN-feature-name/spec.md` 描述「做什么、为什么、验收标准（EARS 语法）、Out of Scope」。不写「怎么做」。
2. **Clarify（澄清）**：主动列出歧义点与边界问题，请人类确认后再前进。
3. **Plan（计划）**：在 `plan.md` 写「怎么做」——架构、数据结构、接口、依赖，须符合宪法与技术栈。
4. **Tasks（拆分）**：在 `tasks.md` 拆成可执行、有依赖顺序的小任务；标注可测试项。
5. **Implement（实现）**：按任务顺序实现，遵循 TDD（核心逻辑先写测试）。一次 3–5 个任务。
6. **Validate（验证）**：对照 spec 的验收标准逐条核验。

## 规则

- 一个功能 = 一个目录 `specs/NNN-feature-name/`（NNN 为三位序号，如 `001-`）。
- 模板见 `specs/templates/`。规格保持 1–3 页，过大则拆分。
- 每阶段边界需人类评审，禁止跳阶段。
- 提交信息关联 spec：`refs specs/NNN-feature-name/spec.md`。
- 详见 `docs/process/spec-driven-development.md`。
