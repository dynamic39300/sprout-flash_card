---
description: 项目核心规则与事实来源指引，始终生效
alwaysApply: true
---

# 核心规则（始终生效）

本项目采用**规范驱动开发（SDD）**。AI 协作的单一事实来源是根目录 `AGENTS.md`，最高原则是 `.specify/memory/constitution.md`。

## 必须遵守（YOU MUST）

1. **先规格后代码**：非平凡功能遵循 SDD 流程（Constitution → Specify → Clarify → Plan → Tasks → Implement → Validate），不得从想法直接跳到编码。
2. **宪法优先**：与 `.specify/memory/constitution.md` 冲突时，以宪法为准。
3. **遇歧义先澄清**：不臆测需求，信息不足先提问。
4. **行为变更先改规格**：先更新 `specs/NNN-*/spec.md` 再改代码。
5. **数据/密钥红线**：严禁提交或破坏 `data/`、`uploads/`、`*.db`、`.env`；密钥走环境变量。
6. **小步交付**：单批实现聚焦 3–5 个任务，保持可评审。

## 技术栈（锁定）

React + Vite + TypeScript（前端，移动优先）· Node.js + TypeScript（后端）· SQLite · 本地磁盘存图 · 阿里云 ECS 部署。未经宪法修订不得更换。

## 参考

- 完整协作规范：`AGENTS.md`
- 需求：`docs/prd/PRD.md` ｜ 技术设计：`docs/tech-spec/TECH_SPEC.md`
- 各类工程规范：`docs/standards/`
